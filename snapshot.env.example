# Copy to snapshot.env and fill in. Not committed.
CW_DIR=/opt/coal-washery-demo
CW_PROJECT=coal-washery-demo
CW_COMPOSE=/opt/coal-washery-demo/deploy/demo/docker-compose.demo.yml
CW_DBNAME=coal_washery_demo

# Where the cw_reader password lives. One line, mode 600, nothing else in it.
PW_FILE=/opt/coal-washery-extract/.cw_reader_pw

# Dated copies older than this are pruned.
KEEP_DAYS=7
